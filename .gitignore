node_modules/
dist/
*.log
.DS_Store

# local daemon cache mirrors ~/.codemaster/<repoId>/ — never commit indexes
.codemaster/

# personal Claude Code settings (permission allowlists); .claude/agents/ stays shared
.claude/settings.local.json

# grep-analysis: generated dumps contain private transcript command text — never commit
tools/grep-analysis/commands.jsonl
tools/grep-analysis/classified.jsonl
tools/grep-analysis/samples/
tasks/.*.tmp
