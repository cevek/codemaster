{
  // Typecheck-only config covering tests (they import src/*.ts directly and run via
  // Node 22 native type stripping — no build step; see test/README.md).
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "outDir": null,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false
  },
  "include": ["src", "test"]
}
