{
  "name": "codemaster",
  "version": "0.0.0",
  "description": "Stateful, always-on codebase inspector for TypeScript/React repos — structural + semantic + refactor queries for AI agents, dense and proof-carrying.",
  "type": "module",
  "bin": {
    "codemaster": "./src/bin.ts"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "examples",
    "ARCHITECTURE.md",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/bin.js",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "knip": "knip",
    "fix-and-check": "eslint src test --fix && knip --fix-type exports --fix-type types && prettier --write . && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json && knip",
    "check": "eslint src test && npm run format:check && npm run typecheck && knip",
    "test": "node --test \"test/**/*.test.ts\"",
    "test:light": "node --test \"test/unit/**/*.test.ts\" \"test/golden/**/*.test.ts\"",
    "test:heavy": "node --test \"test/e2e/**/*.test.ts\" \"test/differential/**/*.test.ts\"",
    "test:unit": "node --test \"test/unit/**/*.test.ts\"",
    "test:e2e": "node --test \"test/e2e/**/*.test.ts\"",
    "test:differential": "node --test \"test/differential/**/*.test.ts\"",
    "test:golden": "node --test \"test/golden/**/*.test.ts\"",
    "test:timing": "node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=./scripts/test-timing-reporter.mjs --test-reporter-destination=/dev/null \"test/**/*.test.ts\"",
    "prepare": "husky"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*.{json,md,js,yml,yaml}": [
      "prettier --write"
    ]
  },
  "keywords": [
    "codebase",
    "inspector",
    "code-intelligence",
    "typescript",
    "react",
    "refactor",
    "codemod",
    "mcp",
    "ai-agents",
    "find-usages"
  ],
  "license": "MIT",
  "dependencies": {
    "@ast-grep/napi": "^0.40.0",
    "@cevek/typescript-extract-refactor-fix": "^6.0.0",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "better-sqlite3": "^11.10.0",
    "chokidar": "^4.0.3",
    "diff": "^9.0.0",
    "picomatch": "^4.0.4",
    "postcss": "^8.5.15",
    "postcss-scss": "^4.0.9",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.19.21",
    "@types/picomatch": "^4.0.3",
    "eslint": "^10.4.1",
    "eslint-plugin-unused-imports": "^4.4.1",
    "husky": "^9.1.7",
    "knip": "^6.15.0",
    "lint-staged": "^16.4.0",
    "typescript-eslint": "^8.60.0"
  }
}
