// trap: the tracked translation function (config functions:['t']). Stub body — the value
// is the call shape, not the runtime. Serves: i18n_lookup / find_missing / find_unused.
export function t(key: string): string {
  return key;
}
