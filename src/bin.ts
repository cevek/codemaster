#!/usr/bin/env node
// codemaster — CLI / process entry point (composition root).
//
// Parses argv, resolves the target repo, then talks to the running daemon over IPC
// (starting it — and the MCP facade — if needed). Implemented in Phase 0
// (ARCHITECTURE.md §17); placeholder until then.
export {};
